class App:

    def __init__(self, app_id, app_name, size, installs, type_id=None, category_id=None):
        self.app_id = app_id
        self.app_name = app_name
        self.size = size
        self.installs = installs
        self.type_id = type_id
        self.category_id = category_id

