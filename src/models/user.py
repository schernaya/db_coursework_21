class User:

    def __init__(self, user_id, username, fullname, registration_date):
        self.user_id = user_id
        self.username = username
        self.fullname = fullname
        self.registration_date = registration_date

